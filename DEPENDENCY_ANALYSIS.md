# PyLinkAgent 依赖支持情况分析

**分析日期**: 2026-04-08  
**更新说明**: 已实现 P0 优先级模块 (Redis, Flask)

---

## 一、依赖分类总览

| 分类 | 依赖数量 | 已支持 | 待支持 | 支持率 |
|------|----------|--------|--------|--------|
| Web 框架 | 4 | 3 | 1 | 75% |
| 数据库中间件 | 5 | 3 | 2 | 60% |
| 缓存中间件 | 4 | 1 | 3 | 25% |
| 消息中间件 | 1 | 1 | 0 | 100% |
| HTTP 客户端 | 2 | 2 | 0 | 100% |
| AI/ML 相关 | 4 | 0 | 4 | 0% |
| 工具库 | 35 | 8 | 27 | 23% |
| **总计** | **55** | **19** | **36** | **35%** |

---

## 二、详细分类分析

### 2.1 Web 框架 (Web Frameworks)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `fastapi` | 0.115.7 / 0.100.0+ | ✅ **已支持** | 已有 fastapi_module 插桩模块 |
| `uvicorn` | 0.34.0 / 0.23.0+ | ✅ **已支持** | 作为 FastAPI 服务器运行 |
| `flask` | 3.1.3 / 2.2.5 | ✅ **已实现** | P0 优先级，已有 flask_module 插桩模块 |
| `gunicorn` | 23.0.0 / 22.0.0 | ⏳ **待实现** | WSGI 服务器，可作为 Flask 配套 |

**相关工作**:
- `PyLinkAgent/instrument_modules/fastapi_module/` - FastAPI 插桩模块
- `PyLinkAgent/instrument_modules/requests_module/` - requests 插桩模块
- `PyLinkAgent/instrument_modules/flask_module/` - Flask 插桩模块 (新增)

---

### 2.2 数据库中间件 (Database Middleware)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `sqlalchemy` | 2.0.22+ | ✅ **部分支持** | 已有 sqlalchemy_module 基础支持 |
| `pymysql` | 1.1.2+ | ✅ **已支持** | MySQL 驱动，配合影子库使用 |
| `flask-sqlalchemy` | 3.1.1 | ⏳ **待实现** | Flask + SQLAlchemy 集成 |
| `redis` | 5.2.1 / 4.5.5 | ✅ **已实现** | P0 优先级，已有 redis_module 插桩模块 |
| `elasticsearch7` | 7.7.1 | ⏳ **待实现** | ES 客户端，优先级 P2 |

**相关工作**:
- `PyLinkAgent/instrument_modules/sqlalchemy_module/` - SQLAlchemy 插桩模块
- `PyLinkAgent/pylinkagent/shadow/interceptor.py` - 影子拦截器
- `PyLinkAgent/instrument_modules/redis_module/` - Redis 插桩模块 (新增)

---

### 2.3 缓存中间件 (Cache Middleware)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `redis` | 5.2.1 / 4.5.5 | ✅ **已实现** | P0 优先级，已有 redis_module 插桩模块 |
| `cachetools` | 5.3.3 | ❌ **不支持** | 内存缓存库 |
| `cacheout` | 0.14.1 | ❌ **不支持** | 内存缓存库 |
| `flask-limiter` | 3.5.0 | ❌ **不支持** | 限流中间件 (基于 Redis) |

---

### 2.4 消息中间件 (Message Queue)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `confluent-kafka` | 2.3.0 | ✅ **已实现** | P1 优先级，已有 kafka_module 插桩模块 |

---

### 2.5 HTTP 客户端 (HTTP Client)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `requests` | 2.32.4 | ✅ **已支持** | 已有 requests_module 插桩模块 |
| `httpx` | 0.24.0+ | ✅ **已支持** | 用于控制平台通信 (依赖要求) |

**相关工作**:
- `PyLinkAgent/instrument_modules/requests_module/module.py` - requests 插桩实现

---

### 2.6 AI/ML 相关 (AI/Machine Learning)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `transformers` | 4.53.0 | ❌ **不支持** | HuggingFace 模型库 |
| `langfuse` | 3.9.1 | ❌ **不支持** | LLM 可观测性平台 |
| `nltk` | 3.9.4 | ❌ **不支持** | 自然语言处理库 |
| `jieba` | 0.42.1 | ❌ **不支持** | 中文分词库 |

---

### 2.7 搜索引擎 (Search Engine)

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `elasticsearch7` | 7.7.1 | ✅ **已实现** | P1 优先级，已有 elasticsearch_module 插桩模块 |

**相关工作**:
- `PyLinkAgent/instrument_modules/elasticsearch_module/` - Elasticsearch 插桩模块 (新增)

---

### 2.7 工具库 (Utilities)

#### 2.7.1 配置与验证

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `pydantic` | 2.6.3+ | ✅ **已支持** | 核心配置验证 |
| `pydantic-settings` | 2.10.1+ | ✅ **已支持** | 配置管理 |
| `pyyaml` | 6.0.2 | ✅ **已支持** | YAML 配置加载 |

#### 2.7.2 安全与认证

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `cryptography` | 44.0.1 | ❌ **不支持** | 加密库 |
| `pyjwt` / `PyJWT` | 2.12.0 / 2.8.0 | ❌ **不支持** | JWT 令牌处理 |
| `pycryptodome` | 3.20.0 | ❌ **不支持** | 加密库 |
| `certifi` | 2024.07.04 | ✅ **间接支持** | SSL 证书 (requests 依赖) |

#### 2.7.3 日志与监控

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `structlog` | 23.1.0+ | ✅ **已支持** | 结构化日志 (核心依赖) |
| `concurrent-log-handler` | 0.9.25 | ❌ **不支持** | 并发日志处理 |
| `psutil` | 6.0.0+ | ✅ **已支持** | 系统指标采集 |

#### 2.7.4 数据处理

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `pandas` | 2.2.3 | ❌ **不支持** | 数据处理库 |
| `numpy` | 1.26.4 | ❌ **不支持** | 数值计算库 |
| `openpyxl` | 3.1.3 | ❌ **不支持** | Excel 处理 |
| `xlsxwriter` | 3.2.0 | ❌ **不支持** | Excel 写入 |
| `lxml` | 5.1.0 | ❌ **不支持** | XML 处理 |

#### 2.7.5 任务调度

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `apscheduler` | 3.11.0 / 3.10.0 | ❌ **不支持** | 定时任务调度 |

#### 2.7.6 网络与通信

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `urllib3` | 1.26.19 | ✅ **间接支持** | requests 依赖 |
| `requests-toolbelt` | 1.0.0 | ❌ **不支持** | requests 扩展 |
| `idna` | 3.7 | ✅ **间接支持** | URL 处理 (依赖) |
| `chardet` | 5.2.0 | ❌ **不支持** | 字符编码检测 |

#### 2.7.7 并发与异步

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `gevent` | 23.9.1 | ❌ **不支持** | 协程库 |
| `wrapt` | 1.15.0+ | ✅ **已支持** | 核心插桩库 |
| `portalocker` | 2.7.0 | ❌ **不支持** | 跨平台文件锁 |
| `concurrent-log-handler` | 0.9.25 | ❌ **不支持** | 并发日志 |

#### 2.7.8 其他工具

| 依赖 | 版本 | 支持状态 | 说明 |
|------|------|----------|------|
| `python-dateutil` | 2.9.0 | ❌ **不支持** | 日期处理 |
| `pytz` | 2023.3 | ❌ **不支持** | 时区处理 |
| `pyparsing` | 3.1.1 | ❌ **不支持** | 文本解析 |
| `pypinyin` | 0.53.0 | ❌ **不支持** | 拼音转换 |
| `jpype1` | 1.5.0 | ❌ **不支持** | Java 桥接 |
| `tenacity` | 8.2.2 | ❌ **不支持** | 重试库 |
| `treelib` | 1.7.0 | ❌ **不支持** | 树结构库 |
| `attrs` | 23.2.0 | ❌ **不支持** | 类生成库 |
| `itsdangerous` | 2.2.0 | ❌ **不支持** | Flask 安全模块 |
| `jinja2` | 3.1.6 | ❌ **不支持** | 模板引擎 (Flask 依赖) |
| `markupsafe` | 2.1.5 | ❌ **不支持** | Jinja2 依赖 |
| `packaging` | 23.0+ | ✅ **已支持** | 版本检查 |
| `esdk-obs-python` | 3.25.3 / 3.23.12 | ❌ **不支持** | 华为 OBS SDK |

---

## 三、优先级建议

### P0 - 高优先级 (核心功能配套) - 已完成 ✅

| 模块 | 依赖 | 原因 | 状态 |
|------|------|------|------|
| Redis 插桩 | `redis` | 影子库配套，使用率高 | ✅ 已完成 |
| Flask 插桩 | `flask`, `flask-sqlalchemy` | 主流 Web 框架 | ✅ 已完成 |

### P1 - 中优先级 (常用中间件) - 已完成 ✅

| 模块 | 依赖 | 原因 | 状态 |
|------|------|------|------|
| Kafka 插桩 | `confluent-kafka` | 消息队列主流 | ✅ 已完成 |
| Elasticsearch 插桩 | `elasticsearch7` | 搜索引擎 | ✅ 已完成 |

### P2 - 低优先级 (特定场景)

| 模块 | 依赖 | 原因 | 工作量 |
|------|------|------|--------|
| MongoDB 插桩 | `pymongo` | NoSQL 数据库 | 3 天 |
| gRPC 插桩 | `grpcio` | RPC 框架 | 4 天 |
| RabbitMQ 插桩 | `pika` | 消息队列 | 3 天 |

### P2 - 低优先级 (特定场景)

| 模块 | 依赖 | 原因 | 工作量 |
|------|------|------|--------|
| 内存缓存 | `cachetools`, `cacheout` | 特定缓存场景 | 2 天 |
| 定时任务 | `apscheduler` | 任务调度场景 | 2 天 |
| AI 可观测 | `transformers`, `langfuse` | AI 应用监控 | 4 天 |

---

## 四、项目当前依赖 (requirements.txt)

```
# 核心依赖
wrapt>=1.15.0              # ✅ 已使用 - 函数包装/插桩
structlog>=23.1.0          # ✅ 已使用 - 结构化日志
pydantic>=2.0.0            # ✅ 已使用 - 配置验证
pydantic-settings>=2.0.0   # ✅ 已使用 - 配置管理

# HTTP 客户端
httpx>=0.24.0              # ✅ 已使用 - 与控制平台通信

# Web 框架
fastapi>=0.100.0           # ✅ 已使用 - Demo 应用
uvicorn>=0.23.0            # ✅ 已使用 - ASGI 服务器

# 数据库支持
sqlalchemy>=2.0.0          # ✅ 已使用 - SQLAlchemy 插桩
pymysql>=1.1.0             # ✅ 已使用 - MySQL 连接

# 可选依赖
packaging>=23.0            # ✅ 已使用 - 版本检查
psutil>=5.9.0              # ✅ 已使用 - 系统指标采集
```

---

## 五、支持矩阵

```
┌────────────────────┬──────────┬────────────┬──────────┐
│       分类         │ 已支持   │  待实现    │  支持率  │
├────────────────────┼──────────┼────────────┼──────────┤
│ Web 框架           │    2     │     2      │   50%    │
│ 数据库中间件       │    2     │     3      │   40%    │
│ 缓存中间件         │    0     │     4      │    0%    │
│ 消息中间件         │    0     │     1      │    0%    │
│ HTTP 客户端        │    2     │     0      │   100%   │
│ AI/ML 相关         │    0     │     4      │    0%    │
│ 工具库             │    8     │    27      │   23%    │
├────────────────────┼──────────┼────────────┼──────────┤
│ 总计               │   14     │    41      │   25%    │
└────────────────────┴──────────┴────────────┴──────────┘
```

---

## 六、总结

### 当前已支持的核心依赖 (19 项)

1. `wrapt` - 插桩核心
2. `structlog` - 日志
3. `pydantic` - 配置验证
4. `pydantic-settings` - 配置管理
5. `httpx` - HTTP 客户端
6. `fastapi` - Web 框架 (已插桩)
7. `uvicorn` - ASGI 服务器
8. `sqlalchemy` - ORM (已插桩)
9. `pymysql` - MySQL 驱动
10. `packaging` - 版本检查
11. `psutil` - 系统监控
12. `pyyaml` - YAML 配置
13. `requests` - HTTP 客户端 (已插桩)
14. `redis` - 缓存 (已插桩)
15. `flask` - Web 框架 (已插桩)
16. `confluent-kafka` - 消息队列 (已插桩) **P1 新增**
17. `elasticsearch7` - 搜索引擎 (已插桩) **P1 新增**

### 待实现的重要依赖 (按优先级)

1. `pymongo` - NoSQL 数据库 (P2)
2. `grpcio` - RPC 框架 (P2)
3. `pika` - RabbitMQ (P2)

---

**更新说明**: P0 优先级模块 (Redis, Flask) 已完成，P1 优先级模块 (Kafka, Elasticsearch) 已完成，支持率从 25% 提升至 35%。
